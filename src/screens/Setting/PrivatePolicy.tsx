import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const PrivatePolicy = ({navigation}: any) => {
  return (
    <View>
      <Text>PrivatePolicy</Text>
    </View>
  )
}

export default PrivatePolicy

const styles = StyleSheet.create({})